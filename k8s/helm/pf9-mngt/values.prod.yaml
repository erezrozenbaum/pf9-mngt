# ===========================================================================
# values.prod.yaml — production overrides
#
# This file is auto-updated by the CI release pipeline (release.yml job:
# update-values).  It contains ONLY image tags and production host settings.
# NO SECRETS belong here.
# ===========================================================================

global:
  imageTag: v1.82.24

ingress:
  host: pf9-mngt.example.com    # set a DNS name, or leave empty for IP-only access
  tls:
    enabled: true

# Per-service image tag overrides (also updated by CI's sed pass).
# Leave empty to use global.imageTag above.
api:
  image:
    tag: ""

ui:
  image:
    tag: ""

monitoring:
  image:
    tag: ""

workers:
  backupWorker:
    image:
      tag: ""
  ldapSyncWorker:
    image:
      tag: ""
  meteringWorker:
    image:
      tag: ""
  notificationWorker:
    image:
      tag: ""
  schedulerWorker:
    image:
      tag: ""
  searchWorker:
    image:
      tag: ""
  snapshotWorker:
    image:
      tag: ""

tenantPortal:
  image:
    tag: ""

tenantUi:
  image:
    tag: ""
  ingress:
    host: ""    # e.g. portal.pf9-mngt.example.com
